import React from 'react';

const Uncle = ({house}) => {
       return (
              <div>
                     <h5>Uncle</h5>
                     <p>House: {house}</p>
              </div>
       );
};

export default Uncle;